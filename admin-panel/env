type: web
    name: paper2real-admin
    env: node
    buildCommand: cd admin-panel && npm install
    startCommand: cd admin-panel && npm start
    envVars:
      - key: PORT
        value: 3002
      - key: NODE_ENV
        value: production